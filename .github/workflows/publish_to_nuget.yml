name: Publish NuGet Package

on:
  release:
    types: [  ]
  push:
    branches: [ release ]

jobs:
  build:

    runs-on: ubuntu-latest
    environment: Testing

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build and Generate Packages
      run: dotnet build --configuration Release --no-restore --output generated_packages
    - name: Test
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: dotnet test --no-build --verbosity normal
      
    - name: Push NuGet Packages
      run: |
        for pkg in generated_packages/*.nupkg; do
          echo "Pushing $pkg..."
          dotnet nuget push $pkg -k ${{ secrets.NUGET_API_KEY }} -s https://api.nuget.org/v3/index.json
        done
